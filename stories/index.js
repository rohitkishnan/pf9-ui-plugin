import React from 'react'
import { storiesOf } from '@storybook/react'
import { action } from '@storybook/addon-actions'

storiesOf('Storybook', module)
  .add('it is working', () => (
    <div>Storybook is working</div>
  ))
